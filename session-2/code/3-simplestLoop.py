# define a sequence
mySequence = 'hello world' # a string is a sequence of characters
for myItem in mySequence: # loop through each item in mySequence, 
    # code at this indent will be run for each item in the loop
    # we will have a variable myItem that will be equal to the current item
    print(myItem)